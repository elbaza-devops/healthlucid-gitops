apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: staging-appset
  namespace: argocd # here we define the namesapce that the ApplicationSet Manifest will be deployed on.
spec:
  goTemplate: true
  goTemplateOptions: ["missingkey=error"]
  generators:
    - git:
        repoURL: https://github.com/healthluciddev/gitops.git
        revision: HEAD
        directories:
          - path: configs/* # this will create variables with all the paths under /configs directory (eg all microservices configs dirs)
  template:
    metadata:
      name: "{{.path.basename}}-staging" # Name displayed in ArgoCD UI (Application name)
      namespace: argocd
      annotations:
        argocd.argoproj.io/manifest-generate-paths: "/configs/{{.path.basename}}/staging"
        argocd.argoproj.io/sync-options: Prune=true
      finalizers:
        - resources-finalizer.argocd.argoproj.io
    spec:
      project: default
      source:
        repoURL: https://github.com/healthluciddev/gitops.git
        targetRevision: HEAD
        path: helm-chart
        helm:
          releaseName: "{{.path.basename}}"
          valueFiles:
            - ../configs/{{.path.basename}}/values.yaml
            - ../configs/{{.path.basename}}/staging/values.yaml
      destination:
        server: https://kubernetes.default.svc
        namespace: staging
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
          - CreateNamespace=true
          # - ApplyOutOfSyncOnly=true
          # - Validate=true
          # - ServerSideApply=true
